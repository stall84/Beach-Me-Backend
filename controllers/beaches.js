
//const Beaches = require('../models/Beaches');

// @description: Get all of the beaches in DB
// @route GET /api/v1/beaches
// @access PUBLIC (no auth)

exports.getBeaches = (req,res,next) => {
    res.send('WELL On our way to GETTING FKN BEACHED!!!');
};

